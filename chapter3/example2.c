#include <stdio.h>

int main(void){
	int i, j;
	float x, y, z;

	// scanf("%d, %d, %f, %f", &i, &j, &x, &y);
	// printf("%d, %d, %f, %f, %f", i, j, x, y, z);
	// scanf("%f", &z);
	// printf("1: %d, 2: %d, 3: %f, 4: %f, 5: %f", i, j, x, y, z);
	int a;
	scanf("%d\n", &a);
	printf("a: %d", a);
	
	return 0;
}
